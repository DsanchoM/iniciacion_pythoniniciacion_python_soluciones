def compute_max_min(values):
    print(f'Sumary\n\tTotal: {sum(values)}\n\tCount: {len(values)}\n\tMax: {max(values)}\n\tMin: {min(values)}')
    
def main():
    compute_max_min([21, 21, 21, 21, 21, 20, 22])
            
if __name__ == "__main__":
    main()# This is file ejercicio_10.py
